
datasource db {
  provider = "postgresql"
  url      = env("ENV_MONGO_URI")
}

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["mongoDb"]
}

// Models

model Person {
  id        String    @id @default(dbgenerated()) @map("_id")
  firstName String
  lastName  String
}
